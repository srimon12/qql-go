# qql-go 0.1.2

Release date: 2026-04-22

## Summary

This release adds local and external inference modes, client-side BM25 sparse vectors, bulk insert, recommend queries, script execution, collection dump, and a cleaner error propagation path for hybrid search.

## Highlights

- **Local mode** — `qql-go connect --inference-mode local` generates dense and sparse vectors client-side via an OpenAI-compatible embeddings API and upserts explicit vectors to any Qdrant instance.
- **External mode** — `qql-go connect --inference-mode external` for remote Qdrant + remote embedding endpoints.
- **BM25 sparse vectors** — client-side sparse generation with Rust-compatible tokenization, length-prefixed FNV hashing, and corpus-level BM25 weighting. Stats are persisted in `~/.qql/corpus/<collection>.json`.
- **Auto-probing embedding dimension** — `--embedding-dimension` is now optional for local/external mode; the CLI probes the endpoint automatically.
- **`RECOMMEND`** — recommend similar points by positive (and optional negative) example IDs with configurable strategy.
- **`INSERT BULK`** — batch insert multiple documents in a single statement.
- **Explicit insert IDs** — `INSERT` and `INSERT BULK` accept explicit `id` fields.
- **`USING SPARSE`** — sparse-only keyword search without dense vectors.
- **`CREATE COLLECTION ... USING MODEL`** — create dense-only collections sized to a specific model.
- **Script execution** — `qql-go execute <script.qql>` for running multi-statement scripts.
- **Collection dump** — `qql-go dump <collection> <output.qql>` for exporting collections as runnable QQL scripts.
- **REPL shortcuts** — `\e <file>` and `\dump <collection> <file>` for script execution and dump from the interactive shell.
- **Connect-time endpoint validation** — the embedding endpoint is test-called during `connect` to surface misconfiguration early.

## Commits

| Hash | Message |
|---|---|
| `2df93e2` | Add parser and AST support for RECOMMEND, INSERT BULK, USING SPARSE, explicit IDs, and CREATE COLLECTION USING MODEL |
| `dab8dee` | Add OpenAI-compatible embedding client for local/external inference mode |
| `78f6c31` | Add BM25 sparse vector generation with Rust-compatible tokenizer and corpus stats |
| `7ede20c` | Integrate local mode into executor, fix error propagation in hybrid search, and fix RECOMMEND on named-vector collections |
| `331e7fa` | Add script execution, collection dump, and REPL support for execute and dump |
| `4da26c3` | Update skill documentation for local mode, BM25 sparse, RECOMMEND, INSERT BULK, and script/dump |
| `f0b6a98` | Update README and add parity plan document for local mode |
| `c98c0d9` | Add ProbeDimension to embedding client for auto-detecting vector dimensions |
| `6c9b0bd` | Validate corpus stats on load and rebuild corrupt stats automatically |
| `fdd718c` | Add corpus stat cleanup on DROP COLLECTION, auto-probe dimension, and connect-time endpoint validation |
| `06abbad` | Update skill docs: --embedding-dimension is now optional with auto-probe |

## Known limits

- `RERANK` is **cloud-only** in this release. Local and external modes reject rerank queries with a clear error.
- Corpus stats accumulate but do not decrement on `DELETE` or `UPDATE`.

## Full changelog

See [CHANGELOG.md](../../CHANGELOG.md).
