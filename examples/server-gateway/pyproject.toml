[project]
name = "qql-gateway-demo"
version = "0.1.0"
description = "qql-go gateway demo — JWT auth, policy enforcement, multi-tenant retrieval"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110",
    "uvicorn>=0.29",
    "PyJWT>=2.8",
    "cryptography>=41.0",
    "httpx>=0.27",
    "rich>=13.7",
]
